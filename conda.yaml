name: du-event-board
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python>=3.10
  - nodejs>=20
  - pyyaml
  - pip
  - pip:
      - pre-commit
      - ruff==0.16.2
      - mypy
      - bandit
      - vulture
      - types-PyYAML
      - douki
      - requests
      - pytest
      - pytest-mock
